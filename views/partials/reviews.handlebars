<div class="reviews">
  {{#each this}}
  <section class="review">
    <div class="meta">
      {{user.username}} on {{format_date created_at}}
    </div>
    <div>
      {{title}}
    </div>
    <div class="review-content">
      {{content}}
    </div>
  </section>
  {{/each}}
</div>